/// Converts domain errors to user-friendly strings for Tauri commands
pub fn to_user_message(error: &str) -> String {
    // Basic error message conversion
    // Can be expanded to handle specific error types
    error.to_string()
}

